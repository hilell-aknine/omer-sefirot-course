# CLAUDE.md — פורטל ספירת העומר

פורטל לימוד וידאו עצמאי על ספירת העומר ועשר הספירות (63 שיעורי יוטיוב).

## Stack
- **קובץ יחיד:** `index.html` — כל ה-HTML, ה-CSS וה-JavaScript בקובץ אחד.
- **אין backend.** אין Supabase, אין הרשמה, אין שרת.
- ההתקדמות וההערות נשמרות ב-`localStorage` של הדפדפן (פר מכשיר).
- עיצוב: Apple Glass / glassmorphism, פלטה מונוכרומטית אפור/לבן/שחור, RTL.

## מבנה התוכן
- `MOD1` (במערך ה-`<script>`) — 14 שיעורי היסוד (מודול 1).
- `OMER` — 49 שיעורי ימי הספירה. הקוד מחלק אותם אוטומטית ל-7 מודולים של 7,
  ומחשב כותרת קנונית לכל יום ("יום N · ספירה שבספירה").
- הכותרת המקורית מיוטיוב נשמרת בשדה `orig` ומוצגת כתת-כותרת.

## איך מחליפים / מוסיפים סרטון
ערוך את המערך `MOD1` או `OMER` ב-`<script>`. כל שורה:
`['YOUTUBE_ID','כותרת','MM:SS']`. סדר השורות = סדר השיעורים.
המיפוי לימי הספירה הוא לפי מיקום בלבד — שמירה על 49 שורות ב-`OMER`.

## פריסה (Deploy)
מפורסם דרך GitHub Pages. לעדכון: דחיפת `index.html` לענף הראשי של ה-repo
(שם ה-repo ב-ASCII בלבד). אפשר להשתמש ב-skill `gh-pages-deploy`.

## הערות
- נתיב התיקייה בעברית + OneDrive — לוודא סנכרון מלא לפני deploy.
- כל 63 הסרטונים אומתו כציבוריים וניתנים להטמעה (oEmbed) — מאי 2026.
